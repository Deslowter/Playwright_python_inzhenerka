# 🎓 Дипломный проект: Автоматизация тестирования
## Playwright + Python + Allure + GitHub Actions

---

## 🚀 Быстрый старт

### 1. Установка

```bash
# Клонирование репозитория
git clone <ваш-репозиторий>
cd Playwright_python_inzhenerka

# Создание виртуального окружения
python -m venv venv

# Активация виртуального окружения
venv\Scripts\activate  # Windows
source venv/bin/activate  # Linux/Mac

# Установка зависимостей
pip install -r requirements.txt
playwright install chromium
```

### 2. Настройка окружения

```bash
# Создайте .env файл на основе шаблона
cp .env.example .env

# Отредактируйте .env файл, указав актуальные данные:
# - BASE_URL (URL тестируемого приложения)
# - EMAIL (логин тестового пользователя)
# - PASSWORD (пароль)
```

**⚠️ ВАЖНО:** Файл `.env` содержит конфиденциальные данные и **НЕ ДОЛЖЕН** коммититься в Git!

**Пример содержимого `.env`:**
```bash
BASE_URL=https://dev.topklik.online/
EMAIL=tester@inzhenerka.tech
PASSWORD=LetsTest!
```

### 3. Запуск тестов

```bash
# Все тесты
pytest

# Только smoke-тесты
pytest -m smoke

# Только E2E-тесты
pytest -m e2e

# Конкретный тест
pytest tests/test_auth.py::test_successful_login

# Запуск на другом окружении (переопределение BASE_URL)
BASE_URL=https://stage.topklik.online/ pytest
```

### 4. Просмотр Allure отчета локально

```bash
# Генерация и открытие отчета
allure serve allure-results
```

---

## 📁 Структура проекта

```
diploma_project/
├── pages/                      # Page Object классы
│   ├── base_page.py           # Абстрактный базовый класс
│   ├── login_page.py          # Страница авторизации
│   └── configurator_page.py   # Страница конфигуратора
│
├── tests/                      # Тестовые сценарии
│   ├── auth/                  # Тесты авторизации
│   │   └── test_auth.py
│   │
│   └── configurator/          # Тесты конфигуратора
│       ├── ui/                # Компонентные UI-тесты
│       │   └── test_toggles.py
│       │
│       └── e2e/               # Сквозные сценарии
│           └── test_full_order.py
│
├── .env.example                # Шаблон конфигурации (КОММИТИТЬ)
├── .env                        # Реальная конфигурация (НЕ КОММИТИТЬ!)
├── conftest.py                 # Общие фикстуры pytest
├── pytest.ini                  # Конфигурация pytest
├── requirements.txt            # Зависимости Python
├── .gitignore                  # Исключения для Git
└── .github/workflows/          # GitHub Actions (CI/CD)
```

---

## ✅ Реализованные тестовые сценарии

### 🔐 Авторизация
1. **Успешная авторизация пользователя**
   - Ввод корректных учетных данных
   - Проверка успешного входа в систему

### 🎨 UI компонентов конфигуратора
2. **Проверка переключателя "Скрыть столешницу"**
   - Включение/выключение видимости столешницы
   - Валидация изменений в UI

3. **Переключение на П-образную столешницу**
   - Изменение типа столешницы
   - Проверка корректного отображения

### 🔄 E2E сценарий: Полная конфигурация заказа
4. **Сквозной сценарий настройки столешницы:**
   - Выбор П-образной столешницы
   - Установка толщины: 4 мм
   - Отключение плинтуса
   - Добавление острова
   - Включение проточек для стока воды
   - Выбор цвета: N-103 Gray Onix
   - Проверка итоговой конфигурации

---

## 🏗️ Архитектура и технологии

### Page Object Pattern
Инкапсуляция логики работы со страницами в отдельные классы для:
- **Переиспользования кода** - локаторы и методы определены однажды
- **Легкости поддержки** - изменения UI требуют правок только в одном месте
- **Читаемости тестов** - тесты выглядят как бизнес-сценарии

### Ключевые архитектурные решения

#### 1. Абстрактный базовый класс
`BasePage` реализован как абстрактный класс (ABC) с обязательным методом `navigate()`:
```python
from abc import ABC, abstractmethod

class BasePage(ABC):
    @abstractmethod
    def navigate(self):
        """Каждая страница должна определить свою логику навигации"""
        pass
```

**Преимущества:**
- Невозможно создать экземпляр базового класса напрямую
- Гарантируется реализация обязательных методов в наследниках
- Явный контракт наследования

#### 2. Конфигурируемое окружение
Все параметры окружения вынесены в `.env` файл:
- BASE_URL - адрес тестируемого приложения
- EMAIL - логин тестового пользователя
- PASSWORD - пароль

**Преимущества:**
- Простое переключение между окружениями (dev/stage/prod)
- Безопасность - конфиденциальные данные не в коде
- Гибкость - можно переопределить через переменные окружения

#### 3. Оптимизированная отчетность Allure
Декораторы `@allure.step` применяются **только к действиям** (action methods), но **не к проверкам** (assertion helpers):

```python
# ✅ ПРАВИЛЬНО: Декоратор на действии
@allure.step("Авторизация: {email}")
def login(self, email: str, password: str):
    ...

# ✅ ПРАВИЛЬНО: Проверка БЕЗ декоратора
def is_logged_in(self) -> bool:
    return self.is_visible(self.LOGOUT_BUTTON)
```

**Преимущества:**
- Нет дублирования шагов в отчете
- Четкое разделение действий и проверок
- Читаемая структура отчета

### Технологический стек
- **Playwright** - современный фреймворк для автоматизации браузера
- **Python** - язык программирования
- **Pytest** - гибкий фреймворк для тестирования
- **Allure** - красивая и информативная отчетность
- **GitHub Actions** - CI/CD автоматизация
- **python-dotenv** - управление переменными окружения

---

## 📊 CI/CD и отчетность

### Автоматический запуск тестов
Тесты выполняются автоматически при:
- ✅ Push в ветки `main` / `develop`
- ✅ Создании Pull Request
- ✅ По расписанию (ежедневно в 4:00 UTC)
- ✅ Ручной запуск через workflow_dispatch

### 🎨 Allure отчеты в режиме онлайн

**Ключевая особенность проекта:** Результаты тестирования автоматически публикуются на GitHub Pages и доступны по ссылке:

🔗 **[https://deslowter.github.io/Playwright_python_inzhenerka](https://deslowter.github.io/Playwright_python_inzhenerka)**

#### Преимущества онлайн-отчетов:
- ✅ Доступны без установки Allure локально
- ✅ История всех запусков сохраняется
- ✅ Удобная демонстрация результатов стейкхолдерам
- ✅ Полная интеграция с CI/CD пайплайном
- ✅ Доступ 24/7 для всех участников проекта

#### Технологический стек отчетности:
- **Allure Framework** - генерация детализированных отчетов
- **GitHub Actions** - автоматизация сборки и deployment
- **GitHub Pages** - хостинг и публикация результатов

---

## 🔧 Дополнительные команды

### Запуск с различными параметрами

```bash
# Запуск с подробным выводом
pytest -v

# Запуск с остановкой на первой ошибке
pytest -x

# Запуск параллельно (требует pytest-xdist)
pytest -n auto

# Запуск с генерацией Allure отчета
pytest --alluredir=allure-results

# Запуск на конкретном окружении
BASE_URL=https://prod.topklik.online/ pytest -m smoke
```

### Работа с отчетами

```bash
# Генерация HTML отчета
allure generate allure-results -o allure-report

# Открытие существующего отчета
allure open allure-report

# Генерация и открытие в одной команде
allure serve allure-results
```

### Работа с окружениями

```bash
# Проверка текущей конфигурации
cat .env

# Переключение между окружениями
echo "BASE_URL=https://stage.topklik.online/" > .env

# Временное переопределение для одного запуска
BASE_URL=https://test.topklik.online/ EMAIL=test@example.com pytest
```

---

## 📈 Метрики проекта

- **Покрытие функциональности:** Авторизация, UI компоненты, E2E сценарии
- **Архитектурный паттерн:** Page Object Model с абстрактным базовым классом
- **Автоматизация:** Полная CI/CD интеграция
- **Отчетность:** Allure с онлайн-публикацией и оптимизированной структурой шагов
- **Поддержка:** Модульная структура, легко расширяемая
- **Конфигурируемость:** Гибкая настройка через .env файлы

---

## 🔒 Безопасность

### Конфиденциальные данные
- Все чувствительные данные (URL, логины, пароли) хранятся в `.env` файле
- `.env` файл добавлен в `.gitignore` и **НЕ коммитится** в репозиторий
- Для команды доступен шаблон `.env.example`
- В CI/CD используются GitHub Secrets

### GitHub Secrets для CI/CD
Для автоматических запусков в GitHub Actions добавьте следующие secrets:
1. `DEV_BASE_URL` - URL dev окружения
2. `STAGE_BASE_URL` - URL stage окружения (опционально)
3. `TEST_EMAIL` - логин тестового пользователя
4. `TEST_PASSWORD` - пароль тестового пользователя

---

## 👨‍💻 Автор

**Нагорный Максим**  
Senior QA Engineer  
📅 Год разработки: 2025

---

## 📝 Лицензия

Проект разработан в рамках дипломной работы по автоматизации тестирования.

---

**Проект демонстрирует:**
- ✨ Профессиональный подход к автоматизации тестирования
- 🏗️ Применение лучших практик и паттернов проектирования (ABC, Page Object)
- 🔧 Гибкую архитектуру с конфигурируемым окружением
- 📊 Оптимизированную отчетность без дублирования шагов
- 🚀 Полный цикл CI/CD с автоматической отчетностью
- 🔒 Безопасное управление конфиденциальными данными
- 📈 Готовое решение для непрерывного тестирования